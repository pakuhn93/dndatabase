const searchForProperty = (arr, prop) => {
    // cycle through arr with forEach or similar
    // check each name property to see if it matches 'prop'
    // if property.name == prop, end the cycle
    arr.find(prop, index);
}

export { searchForProperty };